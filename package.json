{
  "name": "blog",
  "version": "1.0.0",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "prepare": "husky",
    "start": "nodemon --inspect=9230 src/index.js",
    "watch": "sass --watch src/resources/scss:src/public/css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "hiep",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bootstrap": "^5.3.8",
    "express": "^5.2.1",
    "express-handlebars": "^8.0.3",
    "method-override": "^3.0.0",
    "moment": "^2.30.1",
    "mongoose": "^9.0.2",
    "mongoose-slug-generator": "^1.0.4",
    "slugify": "^1.6.6"
  },
  "lint-staged": {
    "src/**/*.{js,json,scss}": [
      "prettier --single-quote --trailing-comma all --tab-width 2 --write"
    ]
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "morgan": "^1.10.1",
    "nodemon": "^3.1.11",
    "prettier": "^3.7.4",
    "sass": "^1.97.1"
  }
}
